<% @articles.each do |article| %>
  <div class="card p-3 shadow-sm m-4 ">
      <h2><%= link_to article.title, article_path(article)%></h2>
      <p class="article-overview"><%= article.content %></p>
  </div>
<% end %>
